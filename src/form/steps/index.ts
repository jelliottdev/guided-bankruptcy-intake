/**
 * Intake steps: re-export from domain module.
 * Step definitions live in allSteps.ts; can be split further into filing, identity, assets, debts, income, etc.
 */
export { ALL_STEPS, getVisibleSteps, getSeedableFields } from './allSteps';
