# Optional: used by scripts that push to GitHub (e.e. deploy).
# Copy to .env and set a Personal Access Token (scope: repo).
# Do not commit .env.

GITHUB_TOKEN=your-token-here

# OCR Backend API (PaddleOCR service)
VITE_OCR_API_URL=http://localhost:8000

# Attom Data Solutions API (Property Snapshots)
VITE_ATTOM_API_KEY=your_key_here
